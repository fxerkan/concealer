# Local preview only — GitHub Pages builds without this.
#   cd docs && bundle install && bundle exec jekyll serve
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "jekyll-remote-theme"
